# Copy this file to `.env` in the project root and fill in your values.
# Never commit `.env` — it is gitignored.

# Market data uses NSEPython (free NSE data) — no API key required.

# --- Telegram alerts (optional but recommended) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
